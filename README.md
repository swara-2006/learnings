React Hooks

1)Usestate hook

setCount(prev=>prev+1)
onClick={()=>{random_color();countChange();}} //Way to add two functions at a time to listener.
onKeyDown={(e)=>{
    if(e.key==='Enter')
    {InputCount();}
    }}/>